{
  "name": "nilegov-stack",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["server", "client"],
  "scripts": {
    "dev": "concurrently -n API,UI -c blue,green \"npm run dev --workspace=server\" \"node_modules/.bin/vite --port 3000 --config client/vite.config.ts\"",
    "build": "cd client && ../node_modules/.bin/vite build && cd .. && npm run build --workspace=server",
    "start": "NODE_ENV=production npm run start --workspace=server",
    "seed": "npm run seed --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
